{
  "name": "get-chitty-cc",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "lint": "eslint . --ext .ts",
    "fmt": "prettier -w .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --reporter basic"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "eslint": "^10.1.0",
    "prettier": "^3.2.5",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "wrangler": "^4.76.0"
  },
  "dependencies": {
    "@chittyos/chittycontext": "github:chittyos/chittycontext#main",
    "hono": "^4.6.5"
  }
}

